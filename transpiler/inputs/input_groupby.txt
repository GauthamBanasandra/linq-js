function linq_query()
{
	var fruits = ['apple', 'mango', 'banana', 'grapes'];
	var new_fruits =
	            from fruit in fruits
				group fruit by fruit.length into fl
				select fl;

	console.log(new_fruits);
}
linq_query();
